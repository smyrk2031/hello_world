Python3.11でfastAPIを使って下記のアプリを作成してほしい
windows11向け、sqlite3
このアプリはローカルで操作するwebアプリです

画面には設定ボタンハンバーガかな(BASIC認証でアカウントadmin, pass: admin)、借用ボタン、返却ボタン、履歴確認ボタンが配置されている

設定ボタン:設定画面が開く
設定画面では、管理部品テーブルが管理されており、列はNo, 基盤, 備品名, 備考, 管理組織, QRコードの認識テキストとなっている。
自由に追記可能。またcsvでフォーマット出力とインポートもできる(インポートするとその設定になる)そのときはDBがリセットするかのアラートが出て、OKを押したらリセットされる。(一応dbのコピーがタイムスタンプ付きで残す)

借用ボタン:借用画面。カメラ起動してQRコード読み取りモードになる
   撮影ボタンでQR画像を撮影すると文字列が表示される(複数連続で読めるように。セルフレジ方式)
    借用者名をキーボード入力して借用/返却ボタンを押す(各備品の状態を見て借用状態なら返却に、返却状態なら借用にする)
    押されたらdbに履歴をつける(dbには全ログが残る
借用時の画像撮影して残す機能も(証拠写真)

返却ボタン:返却画面。借用者名を入力するとその人の借りている一式が表示されて、どれを返却するかをチェック入れられるようになる。
カメラ画像を残す機能も。(証拠写真)

履歴閲覧ボタン:履歴表示画面が表示される(BASIC認証)
    テーブルで設定画面の様に備品が表示される。列には、No, 基盤, 備品名, 備考, 管理組織, 状態(借用/返却), 借用中なら借用者(返却状態なら差返却者), タイムスタンプ, 証拠写真のサムネ(拡大可能!)
列はフィルタやソートができるといいな!
備品名クリックで、その備品に特化した履歴を全部表示できる(テーブル表示モードと、グラフ(横軸日付でフィルタで期間指定できる。もちろん全期間も。))

仕様書やReadme.md, agent向けのagent.mdも作成する事
requirements.txtも欲しい
zipでダウンロードしたい

表示を減らす
0:40
まず必要なスキルファイルを確認してから実装します。


2個のファイルを表示しました
2個のファイルを表示しました
大規模なアプリなので構造を設計してから実装します。


Write requirements.txt
Write requirements.txt


